package project2;
import java.util.ArrayList;
public class Main {
	
	private static int saltRange = 3;
	
	public static void main(String[] args) {
	
			
		
		//Testing for GraphPlotter class
		/*
		GraphPlotter test = new GraphPlotter(0.9, 0.0, 2.1);
		
		
		String temp = "X Value, Y Value, " + "\n";

        for (int i = 0; i < 10; i++) {
          	test.generateY();
            temp = temp.concat("" + test.getX() + ", " + test.getY() + ", " + "\n");
            test.setX(test.getX() + 2);
        }
		test.outputSingleLine(temp);
	*/	
		
		//Testing for GraphSalter class
		/*
		 * 
		 * 
		 * */
		
		/*GraphSalter salt = new GraphSalter(6);
		
	

		
		salt.saltGraph();
		
		for(int i = 0; i < salt.getListSize(); i++) {
			System.out.println(salt.getIndex(i).trim());
		}*/
		
		//Testing for GraphSmoother class
		
		//GraphSmoother smooth = new GraphSmoother(1);
		//smooth.smoothGraph();
		
		
		//Deck testDeck = new Deck();
		//testDeck.shuffleDeck();
		
			
		//for(int i = 0; i < 5; i++) {
		//	System.out.println(testDeck.drawCard().toString());
			
		//}
	
		
		/*ArrayList<Double> temp = new ArrayList<Double>();
		temp.add(1.0);
		temp.add(1.1);
		temp.add(1.2);
		temp.add(1.3);
		temp.add(1.4);
		temp.add(1.5);
		temp.add(1.6);
		temp.add(1.7);
		temp.add(1.8);
		temp.add(1.9);
		temp.add(2.0);
		temp.add(2.1);
		temp.add(2.2);
		temp.add(2.3);
		temp.add(2.4);
		
		for(int i = 0; i < temp.size(); i++) {
			System.out.println(temp.get(i));
		}
		

		System.out.println();
		System.out.println();
		System.out.println();
		
		for(int i = 0; i < temp.size(); i++) {
			if(i > 1) {
				if(i % 2 == 1) {
					
					
					int positivity = (int) (Math.random() * 2);
					if(positivity > 0) {
						temp.set(i, (temp.get(i)) + (Math.random()*saltRange));
					} else {
						temp.set(i, (temp.get(i)) - (Math.random()*saltRange));
					}
					
				}
			}
		}
		
		for(int i = 0; i < temp.size(); i++) {
			System.out.println(String.valueOf(temp.get(i)));
		}
		*/
		
		//Testing for the various distributions methods
		
					//Scopes should be changed before finalization
		
		//DistributionsLibrary distLib = new DistributionsLibrary();
		
			//Private Method Testers
		
		
		
		//Geometric Distribution testing:
		//	System.out.println(distLib.getGeometric(.2, 3));
		

		
			
	}
	

}

